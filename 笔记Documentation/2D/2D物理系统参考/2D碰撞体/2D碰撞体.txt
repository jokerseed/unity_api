2D碰撞体
2D碰撞体组件可定义用于物理碰撞的2D游戏对象的形状。碰撞体时不可见的，其形状不需要与游戏对象的网格完全
相同；事实上，粗略近似方法通常更有效，在游戏运行过程中难以察觉。
2D游戏对象的所有碰撞体的名称都以“2D”结尾。名称中没有“2D”的碰撞体将用于3D游戏对象。
注意：不能混用3D游戏对象和2D碰撞体，也不能混用2D游戏对象和3D碰撞体。

Use Auto Mass
在2D刚体组件上，勾选Use Auto Mass复选框可将2D刚体的质量自动设置为与2D碰撞体质量相同的值。
与2D浮力效应（Buoyancy Effector 2D）结合时，此设置尤其有用。