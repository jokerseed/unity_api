通过动画可以使用Unity的动画系统完全动画化控件状态之间的每个过度。由于可以同时动画化的属性数量很多，因此这是最强大的过度模式。

要使用动画过渡模式，必须将动画组件附加到控制元素。通过单击“Auto Generate Animation”即可自动完成此操作。此过程也会生成一个已经设置状态的动画控制，
并需要对此进行保存。

新的动画控制还可以立即使用。与大多数动画控制不同，此控制还会存储控制过渡的动画，如果需要，还可以进行自定义。

